class AzureLake extends MapPlace {

    constructor(sortie, layout, doors) { 
        super("Azure Lake", "The Sacred Lake in Westfalia", sortie, layout);
        }

    enter = (map, player) => {}

    move = (map, player) => {}

}